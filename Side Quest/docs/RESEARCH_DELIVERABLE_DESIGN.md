# Research Deliverable — the cloud-authored document (design + build spec)

Status: **DESIGN LOCKED 2026-06-30** (Lucas). Built foundation in place; the deliverable build (Slices A/B/C)
is the next work. This doc is the spec so it survives a compact.

## The vision (Lucas, verbatim intent)
Every research project produces ONE document that the **cloud writes end to end** ("cloud models need to be
writing everything"):
- **Page 1 = the PLAN** (objective, approach, the targets, which databases will be checked, what to gather).
- **Then the PRODUCT** — the profiles / findings themselves.
- **Citations + sources** throughout; every claim traceable.
- **Professional, cleanly read.**
Built by the flow: **unify existing research → check ALL databases (known→unknown) → compose the document**,
and it should **grow live** on the Canvas as it's built.

This is the determinism law at the deliverable: the cloud GENERATES the document; the program orchestrates +
guarantees completeness. The deterministic lossless stitch (lib/assemble) becomes the **completeness ORACLE**,
not the visible output.

## Maps to the pillars ([[verifiable-research-track]])
- **Pillar 0** — the PLAN (reviewable, page 1). Intake already recognizes/creates + reads back a one-line
  estimate; this makes the plan a STRUCTURED object.
- **Pillar 1** — citations / source trail (Slice B).
- **Pillar 3** — cloud-authored, completeness-gated document (Slice A).

## Foundation already built (2026-06-30, this session)
- Intake gate (cloud project recognition → creates the run; commit ed44de9 + 2fe80e8 + 0d8f973).
- Confirm + estimate + correct loop (readback + lib/estimate + lib/correction; commit 96a1ae6).
- **KNOWN→UNKNOWN grounding (commit d6dfd0f)** — lib/known + main.gatherKnown: before each org, pull the prior
  dossier record + Zoe's memory DB + the Echo DBs, feed as the foundation; lanes chase only gaps; the merge
  GROWS from the known record (preserve + add). This is the "unify existing + check all DBs" half.
- First-class DB access (localdb + Echo db_query; commit 4ce3137) — the tools known→unknown queries.
- Two-lane deep research (web ∥ structured → merge; commit a1c6163).
- Token sizing: cloud_logic default num_predict=400 is for SHORT CLASSIFICATIONS (intake/correction/tool-route)
  — NOT documents; per-org section writers raised to 1800-2000 (commit d472582). The full-document compose
  (Slice A) needs MUCH higher / CHUNKED limits so nothing truncates.

## The build — three slices, in order
### Slice A — cloud-authored document + plan page (Pillar 0 + 3)
1. **Structured plan** generated by the cloud at project start: { objective, approach, targets[], databases-to-
   check[], facets[], est }. Stored on the focus (focus.<id>.plan). Reviewable (the readback shows it; the
   correction handler can edit it). Becomes page 1.
2. **Cloud composer** (replaces the visible deterministic stitch as OUTPUT): at finalize AND periodically, the
   cloud composes the whole professional document — page 1 plan → org profiles — from the per-org sections.
   COMPLETENESS-GATED: verify every covered org heading is present; patch any the cloud dropped from the
   lossless sections (the stitch is the oracle). CHUNK for large runs (map-reduce compose) so no single call
   truncates; size num_predict to the document.
3. Wire into condenseRun (it already writes the dossier file + emits to canvas) → emit the COMPOSED document.

### Slice B — citations / source trail (Pillar 1)
**DECISION (Lucas, 2026-06-30): route provenance through ECHO's source tools** (reusable across the app,
Echo owns the source store) — NOT a local ledger, NOT extract-only.

Echo's provenance model is DOCUMENT-CENTRIC (introspected 2026-06-30):
- `save_source({original_url, content_md, citing_doc_ids:[docId], frontmatter:{source, collection_date, …}})`
  archives a cited source + writes bidirectional rows to `source_citations`. LIGHT (no disk bundle needed).
- `record_web_source(...)` is the HEAVY path — needs an on-disk Vault/Sources bundle (.html/.png/.notes) first.
  Skip it for now; use save_source.
- `get_sources_for(doc_id)` → the sources cited by a document (the render input).
- `save_document` / `update_document` → the deliverable must be an ECHO DOCUMENT (a doc_id) to anchor cites.

CAPTURE (the part Echo doesn't solve — the operator only returns tool NAMES, not URLs): extract the real
URLs from each pass's raw output + the deep lane's structured-record refs (990/FEC/KG). lib/sources.js (pure)
does the extraction/normalize/frontmatter/render; main.js does the live Echo calls. If extract-from-raw proves
thin in live testing, instrument the operator to emit each URL as it hits it (the more-accurate fallback).

BUILD:
1. lib/sources.js (PURE) — extractUrls(raw)+structuredRefs(deepRaw) → collectSources(); frontmatterFor(src)
   for save_source; renderSourcesSection(normalizedSources) for the doc. + smoke.
2. main.js wiring — at finalize: save the deliverable as an Echo document (→ docId); for each collected source
   call save_source(citing_doc_ids:[docId]); render the Sources section + inline [n] markers from get_sources_for.
3. The composer (Slice A) takes the rendered Sources section so every org section's claims trace to a source.

### Slice C — live-grow display (#2)
1. Re-run the compose on a cadence during the run and UPDATE the Canvas document block as it grows (re-compose
   → saga_canvas_update_block). Reuses the existing canvas emit (studio/canvas_emit) — NO new canvas UI needed;
   it's "more passes of the combining cloud call," which Lucas anticipated. Doubles as the on-track signal +
   confirms the deep/second track is producing.

## The acceptance test (Lucas)
After A–C: a FRESH research prompt should → generate + show a plan → unify existing research + check all DBs
(known→unknown) → build the cloud-authored document (page 1 plan + product + citations, professional/clean) →
display it growing live on the Canvas.

## deer-flow (github.com/bytedance/deer-flow) — pattern validation
Confirms the architecture (plan-propose→review→execute + context reuse + reporter builds the doc). Different
stack (Python/LangGraph) → borrow PATTERNS not code: (a) the plan as a structured, EDITABLE object;
(b) sub-agent fan-out beyond 2 lanes for a hard target (people/funding/litigation/news); (c) aggressive
context reuse (our known→unknown). See also [[project-zoe-canvas]] for the canvas surface.
