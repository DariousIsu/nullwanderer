# Zoe Capability Census — 2026-08-08 (read-only inventory)

Scope: every USER-ORDERABLE capability (chat orders Lucas gives) + autonomous behaviors he relies on.
Sources: `C:\Users\azrae\Desktop\Side Quest\main.js` (~16k lines), `lib/*.js` organs, `docs/`, and the Echo
(NX ECHO) MCP tool families as exposed to Zoe. Rows marked "?" could not be fully grounded in code.
Line numbers are as of this read; the file moves fast.

Entry legend: **chat** = order typed in chat (test-drivable via the inside access port, `lib/test_port.js`,
127.0.0.1:8767, drives the REAL pipeline); **auto** = autonomous, observed on cadence; **UI** = renderer
surface only (IPC, not chat-orderable); **live-ctx** = fires from live context (a link, a paste, a drop).

## A. Conversation & memory

| # | Capability (order, verbatim-style) | Entry | Key code (file:fn) | Artifact + done-surface | Done-definition sketch | Risk notes |
|---|---|---|---|---|---|---|
| A1 | "Who is X / what do we know about Y / what did we say about Z" — factual + shared-history recall | chat | lib/turn_router.js:computeTurnRoute (route=answer); main.js:~6846 object-first recallResult; cloud-writes-the-reply assembly ~8710 | spoken reply only | Reply grounded in HELD objects (Echo pull), screen==DB==spoken, no invented shared history | Cloud-writes-the-reply live-turn proof pending; stale-dossier nodes bleeding into answers (main.js:6858 filter); engine starvation (identity blob 32-44k) |
| A2 | "How's the research going / what are you working on" — status | chat | turn_router route=status; main.js:7614 _isStatusReq via lib/research.isStatusRequest | spoken reply only | Reports the REAL active focus/run state with true counts, never a confabulated progress story | Detectors-vs-comprehension disease (phrasing nets); the "who is Trump → 19 orgs" misroute class (fixed in cascade order, watch regressions) |
| A3 | "Look up the latest X" — live/external lookup | chat | turn_router route=lookup (OPERATOR_OK); main.js:10937 operatorTools (web_search/open_page/browser_read); dispatch cap 8/turn | spoken reply + cited sources | Answer carries live-web facts with real citations; refusals NAME THE DOOR | Search lane = hidden Bing stealth + deep-browse; crawl re-spin ~40%; content firewall sliceable at operator.js:373 |
| A4 | "List the energy-industry contacts we hold in Louisiana" — held-contacts query | chat | turn_router 3.7 route=contacts; main.js:7344 (gatherHeldContacts + lib/contacts_query.select/label/unmetFilters) | canvas table + voice line | Canvas list with grade/type/sector/state filters APPLIED, honest disclosure of unmet filters (county), geo-gap enrichment offer floated | Served local + early (survives cloud outage by design); 271k Puller rows were all person-kind (org backfill one-shot ~main.js:2282); county has no filter field |
| A5 | "Give me the parish contact list" (a roster she already HOLDS) | chat | lib/held_roster.js:recognize (main.js:6965 heldRosterBlock) — outranks the contacts route | knowledge block in reply (+ canvas via A4 suppressed) | The HELD doc's extracted roster is the answer (title match + body table_extract verification), never "the list is empty" | Built off the boot79 failure (doc #8443, 6,694 rows answered "empty"); title-only match forbidden |
| A6 | "What did I just drop on your canvas / did you process those papers?" — doc-QA + canvas awareness | chat | turn_router route=docqa; main.js:6975 CANVAS AWARENESS; lib/canvas_awareness.js | spoken reply only | Names the actual dropped/held docs and their processed state | Semantic retrieval can't match "what's on my canvas" — the deterministic surface is the cure; regression = going blind again |
| A7 | Paste a huge email/text + ask about it — paste intake | live-ctx | main.js:5919 PASTE INTAKE (3570d55) | spoken reply (+ doc landing) | Reply answers THE pasted content (not a stale thread); oversized paste sized to the window, not dropped | Born from the Westmoreland 83k-char paste (27 turns dropped, stale answer); full-document-ingest law: chunk to window |
| A8 | "No — that's wrong, it was Y" — correction of the prior turn | chat | turn_router route=correction; lib/correction.js | spoken ack + corrected memory write | The correction lands on the object (belief revision), and the next recall reflects it | Two-truths disease history; verify the write actually lands, not just the ack |
| A9 | "Can you see my screen?" / attach an image — vision-in | chat / live-ctx | main.js:6224 SCREEN-SIGHT interceptor → screenLib.capture + seeImage; attachment path same seeImage | spoken description | ONE response containing a concrete, specific description of what is actually visible; honest miss on capture failure | Deterministic because the 24B confabulates sight; two-way vision act→see→verify doctrine |

## B. Canvas & documents

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| B1 | "Print all 64 parishes to a fresh canvas doc" — canvas CREATE | chat | lib/artifact_intent.js (model router, intent=canvas_create) → main.js:4815 buildCanvasFromOrder; legacy net lib/canvas_command.js (62ce798) | canvas DOC tab + durable mirror (canvas_docs) + notes + working stamp | A real tab with the ordered content landed via deterministic emit (promiseArtifactEmit) — delivery claim true by construction; narration output REJECTED (rejectEditOutput), nothing lands | The parish-list zero-write failure is the birth defect; create door was UNGUARDED until 08-08 (CONTRACT_INVENTORY #1); pending-cell entries queue to metabolism (main.js:4985) |
| B2 | "Bullet the list / add a column / continue the doc" — canvas EDIT | chat | artifact_intent intent=canvas_edit → main.js:4916 buildCanvasEditFromOrder; regex fast-path + bounded model classifier (fce22e8) | edited canvas block(s) + durable mirror | The named edit applied to the working doc; anaphora ("it", "same document") resolved; reject-and-leave-untouched on narration | 'pullet'→'bullet' class of typo-misses is the cure-shape origin; artifact-session precedence (other nets YIELD, main.js:7181-7257) |
| B3 | "Build me a report on X" — report composed from held material | chat | artifact_intent intent=report → main.js:5036 buildReportFromHeld (+ token search ~5053); compose contract 9c78319 | canvas report + notes file save + cited doc ids; open questions → metabolism (main.js:5113) | Report composed from ≥1 held docs with ids cited, landed on canvas AND saved; explicit report order builds a DOCUMENT (dbb4df9) | Report notes/ save historically never worked (no module fs — latent fix noted 08-07); ranking must not pick transcripts of the ASK (Hartfield 3/8 slots) |
| B4 | "Pull up that list we made" — product pull-up | chat | artifact_intent intent=pullup → main.js:5008 presentHeldProduct; lib/product_ledger.searchProducts/detectAsk (main.js:10083-10196) | the finished product re-presented (canvas/file pointer) + alternates | RETRIEVES the already-made product (never regenerates); alternates offered when ambiguous | PRODUCT LEDGER pull-up gate born from the Louisiana-list regeneration miss (#11102/03); ledger live proof pending |
| B5 | "Package that as a policy brief" — house-style packaging | chat | main.js:6081-6155 PACKAGE VERB; lib/packaging.renderPackaged + selfCheck; htmlToPdfFile | data/packaged/<date>-<slug>.html (+PDF) + 📦 pointer block on source canvas tab + presence notify + completion announce | Branded file exists, self-check OK on re-open, source-reachability verified for cited shapes (research_paper/policy_brief), pointer block landed | Detection is imperative-only; self-check failure produces an honest "unverified" completion line — test that path too |
| B6 | Drop a PDF/doc onto the canvas — ingest + understanding | live-ctx | ipc canvas:drop-doc main.js:4607 → doc decompose + understanding (contract fix at ~1807: narration → understanding dropped, raw doc stands) | doc_store rows + understanding + canvas presence | Whole document ingested (chunk-to-window), understanding landed or honestly withheld; doc discoverable by A6/B3 | Decompose backlog disease (3,211 unread keyed on arrival path); sync decomp can freeze the loop (app-freeze note) |

## C. Research (directed, list-completion, enrich, deep)

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| C1 | "Research X — build out a dossier" — directed research assignment | chat | turn_router route=task → focus.setFromDirective; startDirectedFocusDriver (main.js:11437, directedFocusTick); adaptive tree P0→P4b (docs/ADAPTIVE_RESEARCH_DESIGN.md, db654b8) | notes/directed-N.md + notes/directed-N-dossier.md + canvas sections (condenseRun canvasEmit) + monologue ticks | Accreted, CITED dossier that survives restarts (focus resume main.js:1538-1561); preflight P0 + re-entry audit P4b arm PAPER mode when depth/citations lag | Adaptive-tree live acceptance test (Hartfield P4b) NOT yet run; deliverable query fail (unbounded rows, no HTTP timeout); unguarded producer line was the M6.3 queue |
| C2 | "Wrap it up" / "expand on the think tanks" / "stop" — run controls | chat | wrap main.js:7837 (X-of-N honesty ~7858); expand 7871-7935 (registry-resolved dossier, buildExpandGoal FULL read); stop 7678 directedStopHandled | final dossier on canvas + honest coverage headline; expand = fresh deepening focus | Wrap says "X of N" (never "complete" at 9/64); expand deepens the NAMED prior dossier, not the most recent one | The 9-of-64 "complete dossier" bug is in-code commentary — regression-test it; no-dossier expand → honest note (7930) |
| C3 | "Get me emails for these 12 people" — named-roster fill (+ the list-completion lane generally) | chat + auto | roster-canvas net main.js:7296-7334; lib/roster_intake; fill via runListCompletionPass main.js:15788 + lib/list_complete | canvas TABLE (Name/Org/Email/Confidence/Source) + durable mirror + background focus; cells patch in place | Table lands FIRST, background fill is cite-or-leave-blank one row/pass with confidence + source; blank = could-not-verify, never guessed | List-completion lane code-complete, LIVE PROOF PENDING (memory: watch [list-complete]); deferred rows re-walk on transient (15833) |
| C4 | "Deep dive on X" — premium single-subject exhaustion | chat | main.js:6051-6072 DEEP-DIVE VERB (startDeepDive/stopDeepDive/deepDiveStatus) | one folded dossier; status line on ask | Digs "until the well runs dry" on the premium model, folds sections into ONE dossier; beat rotation holds off | Premium-lane spend — quota governance interaction; status/stop verbs must not misroute |
| C5 | "Swarm <beat>" / "swarm status" / "release swarm" — parallel worker surge | chat | main.js:6160-6184 SWARM verb; swarm quieting main.js:4734-4749 | per-partition dossiers folding in + chat announces from swarm threads only | Workers split target partitions; only swarm threads announce; release returns rotation | The stuck county-commissions-la partition kept the canvas empty 27h (in-code note); partition convergence must be visible |
| C6 | "Find social/online accounts for <Name>" — social enrich | chat | main.js:7437-7454 SOCIAL-ENRICH; lib/enrich_maigret.detectSocialEnrich + runSocialEnrich | CRM card staging (grade E, UNVERIFIED) + voice report | Corroborated public accounts STAGED not promoted; zero-clears = honest "nothing cleared the bar" | UNKNOWN NEVER VOUCHES doctrine; target must resolve to a HELD contact first |

## D. Renders (saga / vault deliverable shapes)

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| D1 | "Give me a quick hit / executive briefing / op-ed / verification / citation pack / draft review on X" — schema-locked deliverable shapes | chat (via operator/condense) + Echo tools | Echo: saga_render_quick_hit / _executive_briefing / _op_ed / _verification / _citation_pack / _draft_review; reached from Zoe through echoSuit.dispatch / operatorTools (main.js:11165) | canvas DOC tab (schema-locked, voice-leak-validated) | Shape renders with cite_floor≥1 enforced by operator policy; content grounded in search_knowledge results | Cite discipline is Echo-side; Zoe-side the condense/organize producers feeding these were the unguarded line (now contracted, CONTRACT_INVENTORY) |
| D2 | "Certify this document" — vault render + citation certification workflow | chat/UI | Echo: vault_render_* (file artifacts) + rainey_open_verification_session → attach findings → compile report → render_citation_certification; Zoe side ipc editor:certify main.js:3573 | branded FILE artifact + certification record + editor tab state | Two lanes complete (citation check then fact check, ADVISORY) with findings attached and a compiled verification report | Editor verification is advisory by design — done-ness is the compiled report existing, not a pass/fail gate |

## E. Forecast & scenario

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| E1 | "What's the House/Senate forecast?" — 2026 midterm forecast machine | auto (recompute loop) + UI + chat answers | lib/forecast_loop.js:runOnce/recompute (registry→poll_average→news reactor→correlated sim→balance payload); ipc forecast:widgets/:balance/:poll-average/:calibration (main.js:2670-2727) | forecast studio widgets + spoken answers grounded in latest run | 435+35 seat chain with parts→whole WORK transparency; unattributable races flagged `illustrative` (no phantom precision); calibration Brier 0.115 | Fail-soft everywhere = silent degradation risk (dead feed → prior); echo resolve deliberately off the hot loop; persistence to memory rail separately gated |
| E2 | "What if <shock> on election day?" — conditional scenario | UI (forecast:scenario, typed or catalog) + auto (autonomy move 'scenario', main.js:12136-12180); chat-order door: ? (none found — reaches it only via autonomy/UI) | lib/scenario_engine.runScenario (pure, same-seed delta); lib/scenario_estimate.buildScenarioFromDescription (cloud estimator); lib/scenario_catalog | delta-vs-baseline payload (UI) / monologue line (auto) — ISOLATED, never memorialized as fact | Same-seed baseline+scenario so delta = shock; ambiguous shocks run BOTH signs (range, direction_uncertain); labeled hypothetical | Smokes in gate since d62ba21; autonomy floor via last_scenario_run_at; a typed chat "run the Iran scenario" has NO deterministic chat door — verify or it lands in converse |

## F. Autonomous lanes

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| F1 | Metabolism — recheck-queue drain of marked gaps | auto (armed at boot, cap/hour) | main.js:2200-2269; lib/recheck_queue (enqueue/defer/stats); daily trend meta metabolism.trend.* | DB rows (queue state) + console trend line + filled canvas cells (B1 pending marks, B3 open questions) | Due gaps drain at cap (default 12/h on gemma4:31b-cloud), trend DECLINING vs baseline 1,908 | Purification-billed-as-idle disease is the origin; deferral must PAUSE not fake-complete (roster-refresh lesson); cap raise = Lucas's call (M9) |
| F2 | Autonomy decision tick — engage / open-advance-close-inquiry / rehearse / explore / build / maintain / scenario / nothing | auto | main.js:11722-12264 (decision.move dispatch); lib/autonomy.historyPush; grounding gates 11758-11772 | unprompted spoken line (engage) / history entries / monologue ticks / artifacts per move | ONE model-chosen move per tick with grounded outcome recorded; engage blocked unless say is grounded in state; interest-follows never framed as shared history | `explore` observed firing with steps=0 artifacts=0 (unproven, defect suspected); move-answer consumer is the LAST open contract row (#10) |
| F3 | Lines of inquiry (incl. mid-chat `<dig>` fork) | auto (chat-seeded) | lib/inquiry.js (open/touch/close, MAX_ACTIVE, park); lib/dig.js (dig tag → inquiry, announce addressed to the talk) | inquiry rows + closed-answered → doc_store artifact (source 'inquiry') + announce-door finding in chat | A question persists across touches with model-written next_step; close writes the answer artifact; dig finding comes home "about the X you asked" | Met-streak refusal forces honest closes (11848); banked digs advance on autonomy tick — a cap defers, never disappears |
| F4 | Rehearsal / R2 self-scripting — needs → sandbox runs → permanent tools | auto (move=rehearse) | main.js:11984-12117; lib/rehearsal.js + rehearsal_driver.js; self-watch→need queue (M2.5.6 queue-jump + reaper) | rehearsal run state + on graduation a permanent R2 TOOL + suite | Study-first opening (narration/zero-URL study → honest "opening unstudied"), iterate to pass, tool lands with tests | R2 card only recently reachable (sandbox fixes 08-07); study contract fixed 08-08 (#9); dupe pile-up fixed 08-04 |
| F5 | Byline — "write/publish a post about X" then autonomous research→write→publish | chat start, auto advance | main.js:6339-6348; lib/byline.js (stages per idle tick; publish via substack_publish web recipe); rejectDraft contract at WRITE + re-check at PUBLISH (#13) | PUBLIC Substack draft/post + creator studio state (ipc creator:*) | Draft passes deliberation/boilerplate/echo/thin rejects, publish recipe runs only on a good file | PUBLIC surface — highest blast radius; bad file → back to write, recipe never runs (verify this loop) |
| F6 | News lane — 189-feed ingest + hourly briefing + email newsletter intake | auto (+ UI news:briefing, feeds:*) | main.js:2339 (hourly brief → Monitors widget); lib/news_brief.js:generateBrief (schema-locked fill + deterministic renderer); email intake main.js:2293-2330; lib/news_store/news_topics; tuner ipc news:tuner-* | Monitors-widget briefing (no chat interruption) + news DB rows | Brief ALWAYS renders (deterministic fallback); stories referenced by OUR ids — model cannot invent a story or a source | Sources last fetched 39d ago as of 08-03 (staleness!); newsletters ride the rail as data, not chat nudges |
| F7 | Roster refresh organ — federal 537 seats + state tier + election-night watch | auto (boot cadence) | main.js:2159-2163; lib/roster_refresh.run + roster_watch + civic_store (+ CRM write-through) | civic_bodies/memberships DB rows + covered-focus stamps + console summary | Seat rosters refreshed from curated sources with stamps on covered focuses; LOCAL tiers stay full-research | v2/v3 + PRODUCT LEDGER gate code-complete but LIVE PROOFS PENDING; the false-validated grinder fabricated 267/270 federal coverage (repaired); ⛔ fuzzy key match dangerous |
| F8 | Nightly self-test through the inside access port | auto (02-06h ET, once/day, idle-gated) | lib/test_port.js:start (main.js:2272-2276, 127.0.0.1:8767 drives runChatTurn); nightly wiring main.js:~13426 | test results (port replies = REAL pipeline output) | A full-pathway chat turn round-trips with a real landed artifact, on the real doors | ZOE_TEST_PORT=0 disables; port is also THE census's own test-drive entry — everything marked "chat" above is drivable here |
| F9 | Quota governance + model-routing self-true-up | auto | quota scrape (main.js:~2073 donor trick + lib/quota_scrape pure parser); tiered subconscious (MERIT_THRESHOLD=2); fleet routing | routing decisions + quota summary (Echo get_quota_summary) | Weekly burn tracked from the SCRAPED page (not by-hand marks); deferral PAUSES work rather than faking completion | Echo hands-slot repin history (deepseek 79→87% burn); fleet config split across .env/meta/hardcoded |

## G. Meetings & media

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| G1 | Paste a meet.google.com link — join, introduce, observe, scribe, leave | live-ctx (+ calendar "Zoe: Join", ipc meet:join) | main.js:6249-6261 → startCanvasMeeting (2969, one funnel for ALL join routes); lib/gmeet stepper; lib/meeting_scribe + meeting_leave (polarity-safe); boot-resume 2524 | meeting notes doc (scribe finalize) + canvas meeting pane + spoken ack | Joins in canvas pane as herself, observes captions, leaves on the 4 triggers (questions/negations never fire), finalized notes land as a document | Leave polarity was the 08-07 fix; scribe boot-resume exists — test a mid-meeting restart |
| G2 | Paste a Teams meeting link — same canvas flow, Teams partition | live-ctx | main.js:6263-6272; lib/teams + teams_canvas driver (2957-2966); partition config 336-347 | meeting notes + canvas pane | Same done-ness as G1 on the Teams DOM | FIRST LIVE FIRE 08-07: join+observe worked but 101/101 caption scrapes EMPTY; fix d7b2c99 live, PROOF = next real meeting; chat composer unfound; SSO CSP-blocked (anon join only) |
| G3 | "Watch this video <url>" / "find clips of X on youtube" | chat | main.js:6274-6307; lib/media_cc (caption-follow stepper, detectSearchWatch + findAndStart) | spoken ack now, caption-grounded talk after; CC stream state | Opens with captions ON and follows the LIVE caption stream; never invents scenes/dialogue; honest miss when no usable clip | Guard against confabulated watching is explicit in the prompt notes — test the no-clip path |
| G4 | "Listen to me / transcribe this call" … "stop listening" | chat | main.js:6309-6337; lib/listen (Echo transcription capture: mic or loopback; record→stop→transcribe) | transcript fed back into her next reply; Echo transcription session rows | Stop returns the REAL transcript segments and she responds to what was actually said; pending/failed = honest note | Full Echo transcription_* suite (voiceprints, diarization, summarize, export) sits behind this — census tests the chat door, the suite is Echo-side |
| G5 | "Record how to X on <site>" … "stop recording" — recipe by demonstration | chat | main.js:6193-6221; detectRecordCommand; webLib.startRecording/stopRecording → saved recipe (provisional until first real run) | saved web recipe (steps) + spoken ack | Steps captured from HIS demonstration and saved; shadowed as review copy if a verified recipe exists | "Never deny the capability" prompt guard — failure must report the ERROR, not a capability denial |
| G6 | "Open X in your browser" — web intent | chat | main.js:6415-6478 WEB-INTENT; detectWebIntent; webLib.open | her stealth browser open on target + spoken ack | The named page actually opens (o.ok); bare "open" with browser already up is a no-op by design | Also the substrate for act-on-open-page (detectActOnOpenPage) and recipes |

## H. Peripheral

| # | Capability | Entry | Key code | Artifact + done-surface | Done-definition | Risk notes |
|---|---|---|---|---|---|---|
| H1 | "Draw me three pictures of X" — local image gen | chat | main.js:7208-7285 draw intercept (yields to artifact session); local SDXL/ComfyUI; cap 4/turn; refine/continuation forms | image(s) landed on canvas + honest count ("made N/M") | Real rendered images on the canvas; imageGenThisTurn check backs the claim (main.js:14464) | Kill-switch ZOE_IMAGE_GEN_ENABLED (hermetic smoke b53749b); renderer/vision.js commit state was once unverified |
| H2 | Calendar surface — view + operator-initiated event writes | UI only | ipc calendar:* main.js:4244-4336; lib/gcal (token via Echo venv, in-memory only); studio/calendar_view | calendar UI + Google Calendar rows | Near-1:1 gcal view; create/update/delete ONLY from the operator's form — never autonomous | Writes are operator-initiated by design; "Zoe: Join" calendar hook funnels into G1 |
| H3 | Editor / verification studio — import, run checks, certify, export, publish | UI only | ipc editor:* main.js:3078-3834 (run-checks 3474 = citation lane then fact lane, ADVISORY; certify 3573; ocr-pages 3733; export-report/doc; publish 3834); lib/editor_checks/editor_cert | editor tab findings + certification + exported files | Both lanes produce attached findings; certify compiles the rainey verification report; exports re-open clean | Advisory by design; OCR/pptx/epub Echo-side formats still open per rich-canvas memory |
| H4 | QR suite — generate/style/save/bulk/analytics | Echo tools only (? no Zoe chat door found in main.js) | Echo qr_generate/_apply_style/_save/_bulk_from_csv/_export_kit/_analytics etc. | QR files + saved records (Echo side) | A styled, validated QR artifact exported | ? — reachable from Zoe only if surfaced through operator tool registry/auto_tools; not observed in main.js — census should test whether a chat order can reach it at all |
| H5 | KG / 3D graph visualization | UI only | ipc kg:overview/:ego/:search/:shortterm/:self main.js:5499-5632; kg3d (nodes bind to VRM vertices); lib/kg_activity | KG panel / kg3d scene | Panel renders live graph + dev-activity; enrichment lands visibly (kg:focus-move 1495) | kg3d alive-budget doc; visualization only — no write path |

---

**Row counts:** A=9, B=6, C=6, D=2, E=2, F=9, G=6, H=5 — **45 capabilities.**

**Most load-bearing but least-tested (test these first):**
1. **C3 list-completion / named-roster fill** — the flagship cite-or-leave-blank promise; code-complete, live proof explicitly pending in memory.
2. **B4 product pull-up (product_ledger)** — the gate that stops silent regeneration of finished work; born 08-07, no live proof.
3. **F7 roster refresh organ (state tier + election-night watch + ledger gate)** — repaired after fabricating 267/270 coverage; v2/v3 live proofs pending.
4. **G2 Teams meeting captions** — first live fire captured 0/101 captions; the d7b2c99 fix has never seen a real meeting.
5. **C1 directed research adaptive tree (P0→P4b)** — the base contract of all research; whole tree built, the live acceptance test (Hartfield P4b) never run. (Close 6th: E2's missing chat door for scenarios — an order Lucas would naturally give that may have no deterministic route.)
