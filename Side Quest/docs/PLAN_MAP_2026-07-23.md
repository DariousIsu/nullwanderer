# Plan map — engine/autonomy lane — 2026-07-23 evening

The remaining plan in full detail, written at Lucas's request ("produce a very detailed map of
your remaining plan and add it to the repo"). This is the successor to the POST-COMPACT AGENDA in
BUILD_HANDOFF_2026-07-22 §⓪⓪ and supersedes its draft ordering. Companion docs: the handoff
(session history + live watches), HARNESS_ORGAN_CATALOG (organ designs), memory ledger
boot44-live-watch (lessons).

**Direction (Lucas, verbatim intent):** "It sounds like we are really starting to smartly
self-build the database — let's focus our remaining time on getting retrieval, deep reasoning,
document printing … and I want the full sandbox suite build being used and tested." Plus the two
standing shape-rules from today: TOP-DOWN containment (universal, never a channel — 02b1b57) and
the parish-structure correction (form first, members second).

---

## 0. Ground truth as of boot72 launch

**Proven live (circuit-proving ledger):**
- Phase 1 ask→answer: news asks answered from her own stream, follow-ups included.
- Phase 2 direction loop: conversation-born inquiries open/advance/close across boots; zombie
  guard REFUSED fires; bounded-bite write-backs; honest expect verdicts on the chose= line.
- Phase 3 verify loop: expect METs → procedures #13/#16 born; constraints land on NOT-mets.
- Inquiry pull (c141cc1): pulled clerk/registrar list decomposed +40 into the graph, then the
  queue in cost order — both open inquiries got their waiting documents.
- Engage lane: gated, grounded, change-only (hot-dog raise was grounded CONTENT; see defect D1).
- Spreadsheet lane: her own downloads (xlsx) extract + decompose end-to-end; legacy .xls ready.
- Escalation ladder + site ledger + access profiles: doors tried in learned order, concessions
  name the map; top-result skip, dup-download gate, dup-decompose gate all fired live.
- Action honesty: her "I've opened the Politico homepage" claim verified REAL in the log.

**Riding boot72 (committed, needs this boot's uptime to prove):**
- 50cfc03 sweep re-entrancy + per-doc in-flight guard (ends the duplicate-mint race).
- 2bcbdf7 dead-host breaker (akiak/akhiok profiles already trip it).
- 02b1b57 top-down containment on DECISION_WANT + touchBrief.
- 7434ba9 leash + inquiry vocab; c141cc1 inquiry pull. Roster #8443 UN-PARKED at launch.

**Live watches open right now:**
- W1 roster chain: [decomp-sweep] reads #8443 guarded-once → office lists complete → inquiry #1
  close → FIRST harvest-born homecoming.
- W2 hot-dog correction: next unprompted engage + whether Lucas's redirect ever reaches the
  follow store (it has not — row 37578 still active/interest).
- W3 structure-map bite: touch 26+ works the police-jury vs home-rule split from banked docs
  (the deep-reasoning probe).
- W4 duplicate-mint debt: nightly dedup/fusion must fuse #7704/#6702/#8348 duplicates — verify
  tomorrow; if the nightly lane misses them, file a targeted dedup pass.

---

> **STATUS (2026-07-23 late evening, updated in place):** R SHIPPED `a39232a` · P3+P4 SHIPPED
> `db5e135` · E SHIPPED `95a100b` — all gate-green, ALL RIDE BOOT73. Still open in P: the live
> "package that" proof + the parish canvas (waits on the roster, decomposing on boot72 now).
> T/D remain proof-through-use. Boot73 after W1 completes (roster → inquiry #1 close → first
> harvest homecoming). Then the live tests: a run-named need → OPENED rehearsal → proposal card;
> a delivery promise → materialized canvas tab; a redirect after a raise → unfollow logged;
> an interest-raise phrased as HER find (or blocked).

## 1. Slice R — the need→rehearsal wire (sandbox suite USED, not just built)

**Why first:** every rehearsal tool shipped in slice 5; live rehearse firings: ZERO — structurally
zero, because `rehearse` only ADVANCES a run and nothing can OPEN one. Lucas's want #4 verbatim:
"I want the full sandbox suite build being used and tested."

**Design (R3 inviolate — nothing self-adopts; proposal cards only):**
1. **Need detector** (new lib/capability_need.js): a pure detector over run findings/learned text
   for the named-capability shape — "I need a tool that…", "requires a script/library…", "cannot
   read/parse X format", "no tool can…". Output: { need, evidence, run_ref }. NOT an LLM pass —
   the shape is narrow; regex + context window, offline-smokeable. The roster episode's "a tool
   that can read XLS files" is the canonical fixture.
2. **Typed need row** (sq.db `capability_needs`): need text, born_from (run ref), status
   (open|rehearsing|proposed|adopted|parked), created_ts. Detector inserts deduped (same-need
   fuzzy match). Retire with provenance, never delete.
3. **Decider state line** (lib/autonomy.js buildManifest): `• CAPABILITY GAPS SHE HAS NAMED:`
   listing open needs with age + born_from. Schema-line law: the moment the state shows a gap,
   the move can exist.
4. **rehearse gains OPEN form** (DECISION_WANT + main.js handler): `rehearse` with target
   "need #N" and NO active run → rehearsal_create seeded from the need (slug from need text);
   need.status → rehearsing. The existing bounded iterate/test/drive cycle then runs it; green
   exits as the existing proposal-card document; need.status → proposed. Guard: at most ONE
   rehearsal run open at a time (existing slot rule); an open need older than N days surfaces in
   the manifest ("stale gap — park or rehearse").
5. **Wire the detector** at the two places needs get named: inquiry write-back (learned/next_step
   text) and one-shot run reports (autonomy history outcome text).

**Acceptance (circuit-proof):** a live run names a need → row appears → manifest line appears →
decider CHOOSES rehearse (count firings in autonomy.history) → run iterates → proposal card doc
exists addressed to Lucas. Smokes: detector shapes (positive + the acknowledgment-negative),
need dedup, manifest line, open-form validation, full offline arc with stubbed rehearsal lib.

**Explicit non-goals:** no self-adoption; no idle-inferred needs (only needs NAMED by failing
runs — Lucas's default, standing unless he says otherwise); no new sandbox tools.

**R-intake (SHIPPED with R):** three intake lanes feed the ONE need store — inquiry write-backs,
one-shot work-move outcomes, and her CONVERSATIONAL says (the original XLS need was named in chat
before any inquiry existed). Both interaction modes reach the sandbox: background opens/advances
runs on idle ticks; in chat the cloud operator can drive rehearsal_* directly on Lucas's word.

## 1b. Slice R2 — the PYTHON lane (Lucas: "can she use all the python loops pre built?")

Two truths, kept distinct:
- **Echo's pre-built Python machinery is ALREADY hers at runtime** — passes, recipes, curation
  loops, spawn_workflow, all 546 tools. No sandbox needed; what it needs is USE (the O0
  starvation thread). Nothing to build here except the habits the state lines already push.
- **NEW Python tools need a sandbox py-lane** (not built): the rehearsal tree is her JS repo copy
  and the test step runs JS smokes under Electron. Design: (1) rehearsal edit/create allowed for
  tools/*.py inside the sandbox; (2) a py test harness — the run's suite may be a smoke_*.js that
  SHELLS the sandbox .py via the Echo venv interpreter (echoVenv.python, already resolved in
  main.js for the gcal bridge) with a bounded timeout + captured output riding the next attempt;
  (3) the proposal card carries the .py + harness + a run transcript; adoption stays Lucas's.
  Suite matching: a need whose tokens fit no existing smoke can BIRTH its harness first (test-
  first) instead of parking — the parked-for-Lucas fallback remains for genuinely unjudgeable
  needs. Sized: one session. Prereq: none — echoVenv is live at boot.

## 2. Slice P — document printing on demand ("package that" proof + delivery-promise net)

**Why:** Lucas: "I still get a canvas doc every time she starts a new county, but never when I
ask for something." The directed driver auto-opens tabs (works); the REQUEST path (papers
pipeline 7f6184d: 4 shapes → branded file + canvas pointer) has never fired live; and her
delivery promises connect to nothing (measured: "I'll keep adding the parish contacts there" →
0 canvas tabs).

**Steps:**
1. **Live proof of the existing path:** after the roster lands, issue (or have Lucas issue) a
   real "package the Louisiana parish contact list" command; fix whatever break the artifact
   chain surfaces (artifact-delivery-chain memory documents the 10 historical breaks — verify
   the ARTIFACT, not the log line).
2. **Parish canvas as first product:** organized per-parish under its ACTUAL governing body
   (roster Office Titles teach the form — parish-structure rule), auxiliaries (sheriff/clerk/
   assessor) listed under the body, emails carried. Cited to the roster doc.
3. **Delivery-promise net** (mirror of the promised-lookup net, main.js): an unprompted or reply
   say containing a DELIVERY commitment shape (canvas/document/file + first-person future) with
   NO packaging/canvas tool tag in the same turn → route the promise through the packaging
   pipeline (same entry "package that" uses), addressed to the conversation. In-progress-shape
   guard like the lookup net (acknowledgments excluded). The promise becomes the action or the
   say is blocked as ungrounded — never a floating "I'll add it".
4. **Clarify misroute fix (small, same area):** the clarify capture currently binds Lucas's
   reply to the ACTIVE FOCUS regardless of topic (measured: "canvas is perfect" → Beaver County
   focus). Gate the capture on topical overlap between the reply and the focus (token overlap
   with focus content; below floor → route to conversation lane instead).

**Acceptance:** one user command produces a branded file + canvas doc verified in
canvas_docs.db; one delivery-promise say auto-produces its artifact; a mismatched clarify no
longer lands on the wrong focus (smoke with the live transcript as fixture).

## 3. Slice E — engage truthfulness (license + belief revision write path)

**Why:** the hot-dog engage — grounded content, FALSE license ("since we last checked" on an
interest-follow) — and Lucas's redirect wrote NOTHING to the store.

**Steps:**
1. **License honesty at composition:** the engage state line for a followed story must carry its
   reason verbatim, and DECISION_WANT teaches: an interest-follow is raised as "this matched
   your interests" — NEVER shared history; only reason='discussed' may say "we". Schema-line
   law: put the allowed phrasings ON the line.
2. **Redirect write path:** a user reply to a raise that redirects/demotes ("focus on X instead",
   "drop that") → story_follow: unfollow or downweight the raised story (the raise carries
   [story #N]; the reply binds to it — no salience guessing). Measured fixture: Lucas's actual
   correction text.
3. **Interest hygiene (investigate, then decide):** how did a hot-dog chain match her interest
   weights? If the cooking chat seeded a food interest, that's working-as-designed but
   miscalibrated for RAISES — raising threshold for interest-follows should be higher than for
   discussed-follows. Measure before changing.

**Acceptance:** next interest-follow raise says "matched your interests"; a redirect measurably
flips the follow row; no raise ever claims unshared history (smoke on composer output).

## 3b. Slice F — the forecast workspace reaches her cognition (SHIPPED `0668d38`; rides boot73)

Lucas's clarification: "the python loops pre built" = THE PROBABILITY MODELS in the forecasting
workspace. Measured: zero forecast mentions in operator.js/cognition.js — the 2026 balance-of-
power machine ran its loop and painted widgets, unreachable from every lane she talks or decides.
SHIPPED: forecast_query (operator tool, both lanes) — chamber toplines + per-race P(win) from the
last completed run; the spec line teaches "read HER model before reasoning from memory" for any
odds/seats question. REMAINING (F2, unsized): a cognition answer-TIER so a chat odds question
auto-reads the model without the operator reaching for the tool; scenario RUNS as a work move
(scenario-engine-design memory); forecast state line for the decider.
**R3 addendum — "making and running her own scripts like you do" (Lucas yes):** the R2 py-lane
covers PERMANENT tools via rehearsal. One-off ANALYSIS scripts (my probe-script workflow) are a
separate lane with a real jail question: analysis needs the DATA, and the rehearsal jail keeps
data/ unreachable by name. Design decision FOR LUCAS before build: read-only DB snapshot vs
restricted query API vs jailed copy. Parked as R3 pending his call.

## 4. Slices T/D — retrieval + deep reasoning (proven through use, not new organs)

- **T1 structure-map bite (W3):** reasoning-over-holdings live test already set on inquiry #1.
  If touch 26+ produces the police-jury/home-rule split, deep reasoning has its first honest
  MET; if it fails, the failure names the missing organ (likely: multi-doc synthesis inside one
  bounded run — then design THAT, not a generic "reasoning" lane).
- **T2 the parish-list ask:** after Slice P, Lucas asks HER for the list — exercises retrieval
  (graph + docs) + reasoning (organize by form) + printing (canvas) in one stroke. This is the
  four-wants integration test.
- **T3 routing holes as they surface:** Bessent-class (imperative/interrogative), clarify
  misroute (fixed in P4), "which Trump" salience (open chip). Fix on live evidence only.
- **T4 email archive chip** (task_04ef1301): 13,620 emails unreadable from any answering
  surface — the single biggest known retrieval reach gap. Parallel-session material.

## 5. Standing debts (not this focus, tracked so nothing silently drops)

- Duplicate-mint fusion verification (W4) — tomorrow's nightly, then a targeted pass if needed.
- smoke_cognition hermeticity: 11/18 calls mocked; finish to 18/18.
- smoke_activity_coverage contention flake (graph lane's suite).
- Site-digest PLAN worker (storage+narration live; no autonomous sweep worker yet).
- Access-profile line into touchBrief (the map exists; the touch prompt doesn't see it).
- Heartbeat worth-it calibration — Lucas's call, unscheduled.
- Cull session application (retire-from-hunt / era-marked memory nodes) when it lands.
- Graceful-shutdown hang (every boot needs force-stop after window close — find what holds the
  tree open; low priority, reboots work).
- ?ref= param normalization in site ledger normalizeUrl.
- Fine-schedule-PDF-class extraction quality (+25 "entities" from a fine table) — curation lane.

## 6. Operating rules that bind all of the above (unchanged)

Circuit-proving: no organ counts until its circuit fires live; count firings in DATA. Schema-line
law. Bounded bites. R3: nothing self-adopts — Lucas + gate + commit. Reserved levers: reboots,
deletions, commits-adoption, brand, unprompted-speech policy, resource policy. `git add` named
files only. Gate = npm test green (flakes verified standalone). All displayed time Eastern. No
artificial caps — defer, never disappear. Top-down containment universal. Needs list outranks
engineering ranks.

**Order:** W1-W4 watches run continuously; R first (build), then P, then E; T proves through use
at each step. Parallel sessions keep their lanes (graph/kg3d, cull, email chip).
