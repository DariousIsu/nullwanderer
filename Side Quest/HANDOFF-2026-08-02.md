# Handoff — 2026-08-02

State anchors: Echo tip `0ae6134` on `chore/worktree-recovery-2026-07-27` (no remote — never branch
off local main). Side Quest tip `b787996` on `feature/idle-passive-intelligence`. Zoe + Echo are
**DOWN** (Echo standalone torn down; port 8765 free). Graph snapshots:
`backups/civic_graph.pre-pass80cl-20260802.db` and `…pre-pass80cl2-20260802.db`.
Quota resets ~2026-08-02 21:00 ET. Board: **12/12 complete.** CourtListener: 168 opinions / 1,878
live edges (rate-capped at ~196/250 requests today; resume next hour).

---

## ⭐ 2026-08-02 DECISIONS EXECUTED (Lucas directed) + the caselaw reconciliation
- ✅**#3 prune stranded CL stubs** (`683c563`): 956 edge-less `cl://` stubs archived+deleted (guards
  held 666 that were canonical-pointers / resolution-members). Integrity 0/0/0. Reversible via
  pruned_entities.
- ✅**#1 purge source-less proposals** (`683c563`): 133,209 empty-source_set `admin` entity_proposals
  archived+deleted; staging now exactly 23,051 single-source. Snapshot isolated.pre-purge.
- ⏳**#1 the 23,051 single-source** — NEEDS A DIRECTION CALL. 23,020 are NOVEL (not in live graph),
  all `admin` import, all real municipal entities (cities/officials/assocs). NO existing tool promotes
  a STAGING proposal (wikidata_org_resolve/person_enrichment enrich LIVE entities only). So "clean up"
  forks: (a) PROMOTE-AND-CHURN — one-time promote the 23,020 into civic_graph marked single-source,
  then existing enrichment churns them (fits your "let it in, mark, churn"; flood = +23K REAL nodes,
  the junk was already purged); or (b) ARCHIVE them like the 132K (loses real data). Rec: (a).
- ⭐⭐**#4 CASELAW RETRIEVAL — IT ALREADY WORKS, and this reveals my #9 was redundant.**
  `general_knowledge.db` (ATTACHED to search_knowledge) holds **22.4M courtlistener opinions** + 1.69M
  caselaw + 60K us_code + state codes (ar/co) + 219K cfr, each a per-source FTS. `search_knowledge(
  source='courtlistener', query=…)` returns from it ("due process" → 100,639 hits). So a research
  process CAN pull caselaw easily today. ⚠Consequences: (1) this session's CourtListener acquisition
  (168 opinions → corpus.db) was REDUNDANT — caselaw was never actually gone; pass80's KNOWN_MISSING
  was about corpus.db, not the search surface. (2) pass80 wired 1,878 concept→opinion edges to the
  TINY corpus.db slice, not the 22.4M in general — the graph's caselaw links are a sliver disconnected
  from the searchable corpus. (3) `wikipedia.db` (deleted this session) is still in [knowledge] config
  → failing attach, and general lacks wikipedia while corpus.db (has it) isn't attached → **wikipedia
  knowledge retrieval is currently BROKEN**. Decisions for Lucas: repoint pass80 at general's
  courtlistener? clean the dead wikipedia.db config + restore wikipedia search (attach corpus.db)?

## 🟢 2026-08-02 DECISION EXECUTION STATUS (Lucas chose all four)
- ✅**#4a wikipedia search RESTORED** (`a9e35ac`): the deleted wikipedia.db left wikipedia knowledge
  search BROKEN. Store now enumerates each `source` inside a shared-articles corpus as addressable,
  filters via a MATCH-first JOIN, and dedups name-collisions preferring general's dedicated FTS.
  Config attaches corpus.db (2.26M wikipedia articles). Proven standalone: source='wikipedia' → 5
  wikipedia-only hits 0.83s; courtlistener resolves to general's 22.4M; realistic queries 0.1–0.55s.
  Takes effect on next Echo boot.
- ✅**#4b repoint pass80 — RESOLVED: do NOT repoint** (measured). general's courtlistener is 22.4M
  opinions but title:"due process"=1,219 / zoning=11,056 hits — both blow past pass80's max_hits=200
  stopword guard (most doctrines would be SKIPPED), and the titles are case-names + cluster STUBS
  (the junk we just pruned). Repointing would yield fewer, noisier edges than the current 168 clean
  landmark opinions. The architecture is correct + complementary: graph = curated landmark links;
  research retrieval = general's full 22.4M via search_knowledge (same canonical opinion URLs).
- ✅**#1 promote-and-churn DONE** (via MCP promote_tenant_proposals, min_conf 0.4, batches): 23,051
  promoted (23,017 new + 34 merged same-name), 0 failed, integrity 0/0/0. Staging now EMPTY. Snapshot
  civic_graph.pre-promote23k. Isolated 86,289 → 109,306 (+23,017, expected — bare-name entities).
- ✅**#15/#2 RELATION-ENRICH LANE BUILT** (`c761e25`) — the missing "relate" capability. Existing
  lanes couldn't drain isolated (wikidata_org_resolve adds a QID not edges; _enrich_entity is a STUB;
  peer-edges only densify). NEW `echo/resolve/relation_enrich.py` + MCP `run_relation_enrichment`:
  for an isolated entity that HAS a wikidata_qid, fetch its Wikidata STATEMENTS (wbgetentities) and
  mint civic edges to entities those statements point at, resolving the object by ITS OWN qid against
  entities.wikidata_qid (anti-collapse — object not in civic → SKIP, never invent). Wikidata
  statement = the citation. Offline harness 13/13. PROVEN LIVE: 268 isolated qid entities → 78
  connected, 106 cited edges, 14s, integrity 0/0/0 (Henry Cabot Lodge EMPLOYED_BY Harvard, American
  Motors HEADQUARTERED_IN Kenosha). Reversible (tag proposed_by='relation_enrich:v1').
  ⚙**Ongoing drain SCHEDULED** (`d9cf661`): the relate pass is now CHAINED to the tail of the
  `resolve_org_qids` huey job — every resolve batch that adds fresh qids immediately runs a bounded
  relation_enrich(limit=200) to connect what it unlocked (guarded on applied>0, fail-soft, same worker
  so it never blocks + survives restart). So on the full Zoe stack the isolated-drain runs
  automatically over time: resolve IDENTIFIES → relate CONNECTS. Manual one-off still available via
  MCP `run_relation_enrichment limit=300`. Chain-fires proven (stubbed-batch wiring test).

## A. In process — turnkey, just needs running

### A1. CourtListener acquisition (finish the layer)
Slice 1 landed: 88 landmark opinions → corpus.db, 0 errors, exactly 95/95 requests; pass80 landed
**1,373 live INVOKES edges** (store = dry-run prediction; integrity 0/0/0). **~30 of 37 doctrine
seeds remain unfetched.** Account tier: 10 req/min · 100 req/hr · 250 req/day. Key in keychain as
`courtlistener_api_token` (canonical resolver name `COURTLISTENER_API_TOKEN`).

Progress: slices 1+2 = **168 opinions** (0 errors), **1,878 live pass54:v1 edges** (integrity 0/0/0).
The fetcher now marks exhausted concepts (`b09d6e2`) so each slice reaches fresh seeds with no wasted
API — proven live (a 2nd run printed `skipping N exhausted concept(s)`). Per slice (≤1/hr, ≤2–3/day;
each run resumes past landed opinions and skips exhausted concepts automatically):
```bash
cd "C:/Users/azrae/Desktop/NX ECHO/nx-echo" && ./.venv/Scripts/python.exe scripts/fetch_courtlistener_corpus.py --apply
```
Used **195/250** requests on 2026-08-02. `--refresh` re-scans all concepts (after a `--per-concept` bump).
After the day's slices, re-land the concept layer (snapshot first, house rule):
```bash
cd "C:/Users/azrae/Desktop/NX ECHO/nx-echo" && ./.venv/Scripts/python.exe pass80_concept_to_corpus.py --source courtlistener --scope anywhere --commit
```
`--scope anywhere` is the measured choice for this source: titles are case names (title scope gave
9 edges); doctrine density in text is healthy (due process 49/88, everything else 5–20/88, nothing
saturating). The pass clears + rewrites its own edges in one transaction, so re-running is safe.

### A2. Link-grounding — PROVEN LIVE 2026-08-02, and the real blocker relocated
Both legs now work end-to-end on the live graph (Echo `43e0e29`, `8ac026f`, `24add0b`):
- **Judge leg ALIVE**: the 200/200 "cloud-unsure" starvation is gone — the cloud makes real
  reject/escalate decisions and the web query carries the packaged evidence (shared neighbor +
  subtype, not two bare names).
- **Verify leg FIXED** (`8ac026f`): it read `page.get("text"/"body"/"content")` — none of which
  `web_fetch` returns (it returns `text_preview`, 2000 chars of RAW HTML = boilerplate). Now pulls
  the FULL readable body via `extract_url` (12k slice). Proven: an escalation's reason went from
  `empty-body` to a real page read — *"Jon Adame appears on this LDA filing… Earl Flood does not"* —
  correctly refusing to ground on a citation that doesn't support the claim.
- **`via` filter added** (`24add0b`): `run_link_grounding(via="CONTRIBUTED_TO")` narrows to one
  meta-path so a drain can sample across the pool instead of pounding the shared-DESC head.

**THE REAL BLOCKER IS CANDIDATE QUALITY, not the judge** (do NOT loosen the judge — reject/escalate
is the anti-over-linking gate working). Two live probes:
- REPRESENTS_ON head = AMGA-cluster lobbyists sharing ~97 clients (shared-TARGET, not a direct tie)
  → correctly rejected, ~0 groundable.
- CONTRIBUTED_TO head = pairs whose other endpoint is WINRED (degree **1855**, a donation PLATFORM)
  or ideological opposites (HUCK PAC ↔ PLANNED PARENTHOOD VOTES) → correctly cloud-unsure.

Root cause: `link_candidates.generate()` excludes mega-hub NEIGHBORS (shared targets, via
`max_neighbor_degree`) but NOT mega-hub ENDPOINTS. See B2 for the fix.

### A3. LDA lane revival (blocked on registration only)
The old API sunset **2026-07-31** — two days before we hit it. Writer fully migrated
(`API_BASE=https://lda.gov/api/v1/filings/`, same `Token` auth per lda.gov/api/redoc/v1; registered
tier 120/min). senate-era keys are NOT honored (filings arrive with empty lobbyists — the writer
now RAISES naming the door instead of reporting a clean zero).
**Door: register free at lda.gov → `nx-echo keys set LDA_API_KEY`.** No code changes needed after.

### A4. Zoe live deliverable proof (Lucas's turn)
The deliverable guide is steered (recipe → echo-find → named read tools → last-resort db_query with
one indexed filter + timeout). Smokes green; the live proof is a real ask: boot her and ask for a
**contact sheet**. The boot log will show the steered path. (Window access is Lucas's — declined by
design.)

### A5. 53-state vote ingest (deliberately parked)
The full library (53 states / 647 zips / 533 with vote CSVs) is turnkey behind `extract_legislation`
— UT pilot green (389 vote relations, 0 errors, fragment guard live). NOT launched: graph-flood is
real. Run state-by-state on Lucas's word, ideally after quota reset. After a few states land:
pass22 → vote_record__c, then re-threshold pass14f/14i (thresholds were measured on AL-only data).

### A6. Background drains (verify occasionally, no action)
- doc-contact sweep: 2 docs/5min, 200 chunks/day, ledger `doc_contacts_scanned` + budget meta in
  sq.db. Newest-first — it must chew through inquiry notes before reaching the 1,468 trapped gov
  addresses. Honest so far (46 scanned).
- decompose sweep: 2 docs/5min, 400 chunks/day, draining.
- Reap rule stands: after any mass entity delete (fold/merge/34b), run
  `scripts/reap_stale_resolution_proposals.py --apply`.

---

## B. Next layer of work

### B1. Corroboration — MEASURED 2026-08-02; the premise does NOT hold. NEEDS A DIRECTION CALL.
I measured every candidate target for a corroboration lane before building. None matches the
handoff's clean premise ("~17K single-source relations, recompute organ exists, just fetch a 2nd
source"):
- **Tenant `relation_proposals`** (data/mcps/rainey/isolated.db): 880 rows, **0 live** (all
  deleted). No target.
- **LIVE `civic_graph.relations`**: 8.7M edges, already promoted/live, `source_set` COLUMN empty
  (provenance in `relation_metadata`). Nothing reads a live edge's confidence as a gate — raising it
  is near-valueless.
- **Tenant `entity_proposals`**: 156,260 rows, ALL live, **155,480 stuck** (below the 0.90 floor).
  BUT: they are ONE `admin` bulk import; **85% (132,429) have an EMPTY source_set** — they need a
  FIRST source, not corroboration; only 23,051 are single-source (a real corroboration target).
- The promotion gate ALREADY corroborates: `auto_promote_grounded` admits a below-floor proposal
  the moment it has ≥2 independent sources. And the live concept-mention path
  (`graph.py:825`) already stamps `source_set=[src]` and accrues sources correctly. So there is NO
  live provenance bug and NO missing recompute organ — the machinery works; there's just no ongoing
  single-source RELATION pile to feed it.
- Existing enrichment lanes already source entities: `echo/refresh_sources/person_enrichment.py`,
  `echo/refresh_sources/wikidata_org_resolve_source.py`.

**Decision for Lucas (pick one; I did not build speculatively):**
1. Point the existing enrichment lanes at the **23,051 single-source** stuck `entity_proposals` to
   add a 2nd source → they auto-promote. (Reuses built tooling; watch graph-flood on promote.)
2. Treat the **132,429 source-less** `admin` proposals as a stale import — clear or archive them
   (they can NEVER promote). Needs your OK on why they were imported.
3. Leave the staging pile as-is; it doesn't block anything live.
Recommendation: (1) scoped small + flood-watched, OR (2) if that import is dead. Either way this is a
data-direction call, not a new organ — the corroboration machinery Lucas already has is sufficient.

### B2. Isolated-entity drain — MEASURED 2026-08-02; the degree-cap idea is REFUTED
⚠ CORRECTION to my prior note: a degree cap on endpoints is WRONG. Measured the endpoint-degree
distribution of all 9,614 pending candidates: the high-degree endpoints are DOMINATED by legitimate
US Senators — Amy Klobuchar (3590), Ted Cruz (2893), Jeff Merkley (2889) — and WinRed (1855) sits
right among them (Hoeven 1861, Boozman 2285). You CANNOT separate infrastructure from real
high-activity legislators by degree; a cap would delete the senators. Do not build it.

The DEEPER finding (why yield is low, and it's not a bug): the meta-path generator surfaces
STRUCTURAL PEERS (A and B play the same role toward the same targets — co-lobbyists, co-donors,
committee co-members), but the grounding gate requires a DOCUMENTED DIRECT tie (A and B have a
citable relationship). Those two sets are largely DISJOINT — structural peers rarely have a direct
citable relationship — so the gate correctly rejects/escalates most of them. This is a ceiling on
the link_candidates→grounding approach itself, not a filter to tune.

Implication: the isolated-entity drain will NOT come mostly from link-grounding. Options for the
next session, in priority order:
1. **Pivot to B1 (corroboration)** — it targets DATA-SOURCED relations (bill sponsorships, filings)
   whose direct tie IS citable against an official record, so it does not hit the structural-peer
   ceiling. Higher expected yield. (See B1.)
2. If still draining link-candidates: diversify by `via_relation` (the new filter, `24add0b`) and
   accept that yield is a small fraction; add an escalation-park so unsure candidates don't re-burn
   cloud every run (currently they stay `pending` and re-enter forever).
3. A conduit denylist (WinRed/ActBlue by external_id) is a marginal cost-saver (~1.5% of the pool)
   — the gate already escalates them correctly, so it is optional, not a correctness fix.

### B3. The 1,622 "stranded court_opinions" — IDENTIFIED: shard-era parenthetical/cluster STUBS
⚠ Investigated properly 2026-08-02 (I was wrong twice before — prune, then "named opinions"). What
they actually are: shard-era CourtListener **parenthetical + cluster STUBS**, not opinions —
names like `"Parenthetical for CL opinion #2454382"`, external_id `cl://parenthetical/…` and
`cl://cluster/…`. A parenthetical is a one-sentence CL summary snippet; these are dangling
references to opinions we don't even hold. They are DISJOINT from the real re-acquired opinions
(0 name overlap; new opinions key on `courtlistener.com/opinion/…` URLs), so **re-acquisition does
NOT re-link them** — confirmed: slice-2 land (168 opinions, +56 entities) left the edge-less count
at exactly 1,622. `prune_empty` correctly refuses them (they carry an external_id = identity
context). Whether to prune 1,622 snippet-stubs is a deliberate Lucas call — they assert little but
aren't nothing, and I've misjudged this set twice, so no unilateral delete. If pruned, do it by the
`cl://parenthetical/` + `cl://cluster/` external_id prefix (precise), snapshot first, archive-then-
delete. NOT blocking anything; they just sit in the isolated pool.

### B4. Organization research lane
All 271,334 Puller targets are person-kind; org targets burn tokens through a person-shaped
pipeline and block real research. Needs its own lane shape (org dossier ≠ person dossier).

### B5. pass80 truth-in-logging — ✅ DONE (`0ae6134`)
Fixed 2026-08-02: the commit path counted `len(batch)` (IGNOREd collisions inflated it — printed
"2,079" vs 1,878 landed). Now reads `COUNT(*) WHERE proposed_by=?` in-txn before COMMIT. Proven by
re-run (idempotent): prints 1,878, matching store truth. Same class as pass14/15/29b/49/61-64.

### B6. Known parked items (unchanged)
- 29,078 name-strong/fuzzy resolution proposals: correctly owned by corroborated-nightly + operator
  gate. Not a bug.
- Event-ingest lane #24: built, gated OFF; slugs pending Lucas.
- Alias vocabulary store (alias_* in electoral.db): data exists, consumer unbuilt.
- Contacts short-term gap: crm-door + sweep now drain it; verify `gatherHeldContacts` third source
  appears in a live contact-sheet ask (ties to A4).

---

## House rules that bit us this week (keep honoring)
- Key VALUES never touch shell args/files/logs — keychain via env-var-scoped `keychain_set`.
- Snapshot (SQLite online backup API, never file copy) immediately before every landing run.
- Printed counters lie under triggers — the only truth is `SELECT COUNT(*) ... WHERE proposed_by=?`.
- Refusals name their door; a clean zero must never be achievable by absence.
- `git add` NAMED files only; commit messages via `git commit -F <file>`.
- uv venv = trampoline: every python spawn is TWO python.exe; never kill the "duplicate".
