Kubernetes rolling deployment: set spec.strategy.type to RollingUpdate with maxSurge at 25% and maxUnavailable at 0% for zero-downtime (requires a working readinessProbe). Roll back with kubectl rollout undo deployment/name. Canary: two Deployments with a weighted Service split (10/90) via ingress annotations or Argo Rollouts. Blue-green: maintain two namespaces and swap the Service selector for an instant cutover without traffic overlap during transition.