Terraform import for existing infrastructure: run terraform import resource_type.name resource_id, then run terraform plan to see the diff between the imported state and the desired config. Reconcile config to match actual state, or plan the changes needed to align them. For bulk import, use the import block (available in Terraform 1.5+) with for_each iterating over a resource list. Always review the full plan before running apply after any import operation.