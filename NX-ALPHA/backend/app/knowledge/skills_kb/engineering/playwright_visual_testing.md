Playwright visual testing: use expect(page).toHaveScreenshot('name.png', {threshold: 0.2, maxDiffPixels: 100}) for pixel comparison. Update baseline snapshots with --update-snapshots. Use clip to test a specific component region. Mask dynamic areas like timestamps and ads using the mask array option. Store snapshots in git. CI baseline: generate on main branch and compare on all PRs. Combine visual testing with accessibility testing using axe-playwright in the same test pass.