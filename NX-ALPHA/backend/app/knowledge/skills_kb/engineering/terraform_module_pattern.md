Terraform module design: modules expose only the variables callers need — minimize the interface. Use locals for internal computed values to keep the variable surface clean. Output everything callers might reasonably need downstream. Pin module versions with ~> for minor update flexibility only. Host modules in the Terraform Registry or a private TFE registry. Test with Terratest (Go) or terraform-compliance. Use terraform-docs to auto-generate module documentation from source.