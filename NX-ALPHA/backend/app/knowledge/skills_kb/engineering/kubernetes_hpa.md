Kubernetes HPA config: HorizontalPodAutoscaler with targetCPUUtilizationPercentage of 70 or custom metrics via Prometheus adapter. Set minReplicas and maxReplicas bounds. Use scaleDown stabilizationWindowSeconds: 300 to prevent flapping during load spikes. Use VPA for vertical scaling of stateful workloads. Use KEDA for event-driven scaling based on queue depth or cron schedule. Test HPA behavior under load with kubectl run load-generator before relying on it in production.