CircleCI pipeline patterns: use orbs for reusable config covering node, python, and aws-s3. Choose docker executor for standard builds, machine executor for GPU workloads. Set parallelism: N to split tests by timing data across containers. Use persist_to_workspace for passing artifacts between jobs. Gate production deploys with an approval job. Use contexts for environment-scoped secret injection. Use dynamic config for monorepo path-based conditional job triggering.