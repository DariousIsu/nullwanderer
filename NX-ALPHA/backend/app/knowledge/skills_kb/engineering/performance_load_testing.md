Load testing with Locust: define a User class with @task decorated methods, add on_start for authentication, and set wait_time using constant or between helpers. Run headless with: locust --headless -u 100 -r 10 --host. Track p50, p95, and p99 latency alongside error rate and RPS at capacity. Use a step load test to identify the breaking point. Always compare before and after optimization runs at the same target RPS to validate improvement objectively.