Playwright flaky test fixes: replace all time-based waits with expect(locator).toBeVisible() or waitForResponse. Only use networkidle for legacy SPAs — prefer DOMContentLoaded. Locator strategy priority: role first, then text, then test-id, then CSS, then XPath as a last resort. Debug flakiness with --retries 3 --reporter html to capture patterns. Use the trace viewer via npx playwright show-trace for step-by-step visual debugging of intermittent failures.