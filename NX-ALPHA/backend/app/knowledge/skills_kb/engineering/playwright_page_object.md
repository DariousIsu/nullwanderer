Playwright Page Object Model: each class receives a Page instance in its constructor and exposes action methods rather than locators. Keep all locators private to the class. Async methods return void or data — never page elements. Build a base page class for shared actions like navigate and waitForLoad. Test files interact exclusively through page object methods — no raw locators in test code. This pattern reduces maintenance burden significantly when UI structure changes.