GitHub Actions matrix builds: use matrix.os crossed with matrix.python-version for cross-platform coverage. Cache pip dependencies with key based on runner OS and requirements.txt hash. Use concurrency groups to cancel superseded PR runs. Define reusable workflows with workflow_call triggers for DRY pipelines. Use OIDC token-based auth for AWS and GCP — no long-lived secrets stored in Actions. Use self-hosted runners with GPU access for ML workloads.