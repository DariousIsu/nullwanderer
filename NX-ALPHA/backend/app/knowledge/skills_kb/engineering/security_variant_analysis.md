Variant analysis workflow: start from a known vulnerability (CVE or bug class), write a CodeQL or Semgrep query targeting the root cause pattern, run it across the full codebase, triage matches by data flow reachability, then fix all variants in a single coordinated patch. Document the query for future regression detection. This approach is essential after finding any injection, deserialization, or authentication bypass vulnerability class in a codebase.