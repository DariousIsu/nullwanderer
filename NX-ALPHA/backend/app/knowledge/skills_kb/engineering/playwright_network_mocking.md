Playwright network mocking: use page.route('/api/**', route => route.fulfill({json: fixture})) for static mock responses. Use route.continue({headers: modified}) for request header modification. Use page.waitForRequest and waitForResponse for network assertions. Record and replay with HAR: page.routeFromHAR('fixture.har'). Mock auth endpoints separately from data endpoints to keep fixtures composable. Reset all routes between tests using page.unrouteAll() in afterEach hooks.