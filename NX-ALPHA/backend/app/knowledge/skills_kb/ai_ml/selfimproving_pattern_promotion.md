Pattern promotion from fast-path cache: instrument response pipeline to capture query-response pairs with user feedback scores. After accumulating 50+ high-scoring pairs, cluster by embedding similarity using threshold 0.88 — each cluster represents a recurring query pattern. Promote cluster centroid to Tier 1 cache with an LLM-generated canonical response synthesized from top-rated cluster members. Tag with creation date and mark stale after 90 days — re-evaluate on fresh inputs before renewal. Track cache hit rate and response quality delta vs uncached to validate promotion decisions. Demote patterns whose quality score drops below 3.5 after user feedback accumulation. Store promotion audit trail for transparency.