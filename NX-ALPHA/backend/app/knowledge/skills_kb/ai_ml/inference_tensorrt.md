TensorRT-LLM optimization: build engine with trtllm-build --checkpoint_dir ckpt --output_dir engine --max_batch_size 8 --max_input_len 2048 --max_output_len 512 --gemm_plugin float16. Enable fp8 quantization for Hopper/Ada GPUs with --quantization fp8 for maximum throughput. GptAttentionPlugin enables paged KV cache. Run inference with tensorrt_llm.runtime.GenerationSession(engine_dir). Best for fixed-batch production serving where engine rebuild is acceptable on model update. Integrates with Triton Inference Server for scalable deployment. Throughput gains over vLLM are marginal for dynamic batching but strong for fixed-shape batch workloads.