Self-improving agent memory curation: after each session extract entities, decisions, and outcomes as structured facts with source_session and timestamp. Score each memory entry by downstream utility — did retrieval of this memory improve response quality on subsequent similar queries? Use an LLM judge to rate retrieved-memory impact on a 1-5 scale. Prune entries below score 2.0 after 30 days of non-improvement. Promote high-signal items to a graph memory layer for relational access. Deduplicate by semantic similarity threshold 0.92 — merge duplicates into a single canonical entry preserving the highest-confidence version. Store pruning decisions as audit log for debugging memory drift.