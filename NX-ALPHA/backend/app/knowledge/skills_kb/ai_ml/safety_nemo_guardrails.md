NeMo Guardrails Colang configuration: define user and bot message patterns in .co files with "define user ask_harmful" and "define bot refuse" blocks. Define flows connecting them: "define flow safety: user ask_harmful -> bot refuse". Load with config = RailsConfig.from_path("./config") and wrap your LLM with rails = LLMRails(config). Call await rails.generate_async(messages=conversation). Supports topical rails (keep conversation on topic), safety rails (block harmful content), and jailbreak resistance. Each conversation session maintains its own state. Colang 2.0 adds Python-interop for complex conditional flows. Works with any OpenAI-compatible LLM backend.