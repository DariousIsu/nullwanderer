DSPy prompt optimization: define a Signature class with typed input/output fields and docstring describing the task. Use dspy.ChainOfThought(signature) for reasoning tasks. Compile with BootstrapFewShot(metric=your_eval_fn).compile(module, trainset=train_examples) — optimizer selects best few-shot demos automatically. Call compiled module like a function: result = compiled_module(question=q). Replaces hand-crafted prompts with data-driven selection. Use dspy.OpenAI or dspy.OllamaLocal as the LM backend. Evaluated programs are serializable for reuse.