LlamaGuard content moderation: load Meta-Llama/Llama-Guard-3-8B via HuggingFace transformers. Format input as a conversation dict with role/content pairs following the LlamaGuard chat template. Run inference — model outputs "safe" or "unsafe\nS1,S3" where S-codes map to violation categories (S1=violent crimes, S2=hate, etc.). Use as both input gate (screen user messages before LLM) and output filter (screen LLM responses before delivery). GPU latency is approximately 100ms for a single classification. Supports custom safety taxonomy by modifying the system prompt categories. Integrate as middleware in your API layer for transparent safety enforcement.