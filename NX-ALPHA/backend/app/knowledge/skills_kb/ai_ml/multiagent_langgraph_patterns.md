LangGraph multi-agent patterns: define state with TypedDict including messages list and any shared context fields. Build graph with StateGraph(MyState), add_node("agent_name", agent_fn) for each agent. Route with add_conditional_edges("node", router_fn, {"next_node": "next_node", END: END}). Compile with graph.compile(checkpointer=SqliteSaver.from_conn_string(":memory:")). Add interrupt_before=["human_review"] for human-in-the-loop pause at any node. Use PostgresSaver for persistent cross-session state. Stream events with graph.astream_events(input, config, version="v2") for real-time progress. Subgraphs allow nested agent teams with their own state schemas.