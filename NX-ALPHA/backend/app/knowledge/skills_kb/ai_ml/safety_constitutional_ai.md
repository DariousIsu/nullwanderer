Constitutional AI implementation: define a principles list (e.g., "do not provide instructions for harm", "be honest about uncertainty"). For each model response, run a critique pass: prompt the model to evaluate its own output against each principle and identify violations. Then run a revision pass: prompt the model to rewrite its response to satisfy all critiques. Implement as two sequential LLM calls — critique prompt returns structured violations, revision prompt returns improved text. Can be batched across multiple principles in one critique call. Reduces harmful outputs without requiring labeled harm data. Combine with a reward model trained on revised vs original pairs for RLHF signal.