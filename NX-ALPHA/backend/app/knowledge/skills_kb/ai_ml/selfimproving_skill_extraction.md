Skill extraction from completed task traces: prompt an LLM to identify reusable procedures within successful execution traces, structuring each as (trigger_condition, steps[], success_criteria, required_tools). Filter for generalizability — skip one-off fixes and environment-specific patches. Store extracted skills with embeddings for semantic retrieval on future similar tasks. Re-test each skill on 3-5 held-out similar tasks before promoting to active skill bank — reject if success rate below 0.7. Version skills and track performance drift over time. Tag by domain (code, research, writing) and complexity (atomic, composite). Composite skills that chain atomics should store dependency graph for execution planning.