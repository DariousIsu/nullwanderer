Terraform AWS provider scaffolding: provider "aws" block with region and assume_role for cross-account access. Use an S3 backend with bucket, key, region, and dynamodb_table for state locking. Use data "aws_caller_identity" to reference the account ID in resource definitions. Pin the required provider version to ~>5.0. Apply common tags to all resources via locals.common_tags covering env, project, and terraform=true. Enable aws_default_tags in the provider block for automatic propagation.