Terraform GCP provider scaffolding: provider "google" with project and region fields set. Use google_project_service resources to enable required APIs before dependent resources are created. Create a service account with google_project_iam_member assignments following least-privilege. Provide credentials via the GOOGLE_APPLICATION_CREDENTIALS environment variable. Use data "google_client_config" to obtain an auth token for the kubectl provider. Always pin google and google-beta provider versions together to avoid compatibility drift.