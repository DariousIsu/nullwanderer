EKS Terraform module: aws_eks_cluster with a pinned Kubernetes version, aws_eks_node_group with instance types and scaling config, IRSA via OIDC provider for IAM Roles for Service Accounts, and aws_auth_configmap for Kubernetes RBAC mapping. Choose managed node groups for stateful workloads and Fargate for serverless batch jobs. Required add-ons: CoreDNS, kube-proxy, vpc-cni, and EBS CSI driver. Pin all add-on versions explicitly to prevent unexpected upgrades during apply.