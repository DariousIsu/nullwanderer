Cloudflare Pages deployment: connect via git integration or deploy directly with wrangler pages deploy dist/. Configure build settings using a framework preset or a custom build command. Set environment variables per deployment context — production and preview are independent. Place Pages Functions in the /functions directory using the same Workers API. Use _headers and _redirects files for HTTP response rule configuration. Free tier includes unlimited requests, 500 builds per month, and custom domains with automatic SSL provisioning.